use prometheus_client::registry::Registry;

#[allow(dead_code)]
pub struct MetricsTracker {
    registry: Registry
}

impl MetricsTracker {
    #[allow(dead_code)]
    pub fn new() -> Self {
        todo!()
    }
}
